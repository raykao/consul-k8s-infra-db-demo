{
  "data_dir": "/opt/consul",
  "config_dir": "/etc/consul.d",

  "advertise": "${instance_subnet_ip}",
  "bind": "${instance_subnet_ip}",
  "client": "127.0.0.1",
  "boostrap_expect": 3,
  
  "log_level": "INFO",

  "enable_syslog": true
}